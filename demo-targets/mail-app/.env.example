# -----------------------------------------------------------------------
# Mail App — Environment Variables
# Copy this to .env and adjust values before running docker compose up
# -----------------------------------------------------------------------

# PostgreSQL credentials (used by both db and roundcube services)
DB_NAME=roundcube
DB_USER=roundcube
DB_PASSWORD=roundcube
